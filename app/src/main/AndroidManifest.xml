<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.xxxan.myapplication">

    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <uses-permission android:name="android.permission.USE_FINGERPRINT" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Decrypted1Activity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Decrypted2Activity" />
        <activity android:name=".Decrypted3Activity" />
        <activity android:name=".Decrypted4Activity" />
        <activity android:name=".Decrypted5Activity" />
        <!-- <activity android:name=".StartActivity"></activity> -->
        <activity
            android:name=".Decrypted6Activity"
            android:label="@string/enterCodedManually"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Decrypted7Activity"
            android:label="@string/decryptMessage"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Decrypted8Activity"
            android:label="@string/auth"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Decrypted9Activity"
            android:label="@string/title_page6"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Decrypted10Activity" />
        <activity android:name=".Decrypted11Activity" />
    </application>

</manifest>